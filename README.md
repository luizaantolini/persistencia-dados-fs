# Persistência de dados com Fyle System

Projeto simples que demonstra persistência de dados em arquivo usando Node.js, Express e o sistema de arquivos.

**Descrição:**
- Exemplo pequeno que lê e grava dados em data/livros.json por meio de funções em src/manipula-json.ts e um servidor em src/app.ts.